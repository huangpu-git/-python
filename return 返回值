返回值
return 返回值要点：
1. 如果函数体中包含 return 语句，则结束函数执行并返回值；
2. 如果函数体中不包含 return 语句，则返回 None 值。
3. 要返回多个返回值，使用列表、元组、字典、集合将多个值“存起来”即可。

【操作】
def add(x,y):
    print("计算两个数的和:{0},{1},{2}".format(x,y,(x+y)))
    return x+y
c = add(30,40)
print(c) 

#执行结果是：
计算两个数的和:
30,40,70
70

【操作】定义一个返回两个数平均值的函数
def my_avg(a,b):
    return (a+b)/2
c = my_avg(10,20)
print(c)
#执行结果是：
15.0

